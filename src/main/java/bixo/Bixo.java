package bixo;

import cascading.cascade.Cascade;
import cascading.flow.Flow;
import cascading.flow.MapReduceFlow;
import cascading.pipe.SubAssembly;

public class Bixo implements Runnable {


    @Override
    public void run() {
        // the bixo loop
        while (true) {
            
            // get urls to fetch from db
            
            // scrore url
            // group by PLD
            // fetch 
            
            MapReduceFlow mapReduceFlow = new MapReduceFlow(null);
//            mapReduceFlow.
            
//            SubAssembly
            // extract content
            // extract new urls
            // update urls new and fetched onces
            // MapReduceFlow
            
            // update urls in db
            
            
        }

    }

}
